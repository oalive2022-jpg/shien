{
  "name": "shien-kiroku",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server/migrate.js && node server/index.js",
    "migrate": "npm install --prefix server && node server/migrate.js",
    "dev:server": "npm install --prefix server && node server/index.js",
    "dev:client": "npm install --prefix client && npm run dev --prefix client"
  }
}
